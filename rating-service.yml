server:
  port: 8082

spring:
  application:
    name: rating-service

    spring.datasource.url=jdbc:mysql://localhost:3306/rating_microdb
spring.datasource.username=root
spring.datasource.password=Deepali@decec


# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

    eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
